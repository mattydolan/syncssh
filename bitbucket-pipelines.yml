pipelines:
  default:
    - step:
        name: Nuget Restore
        image: mcr.microsoft.com/dotnet/core/sdk:3.1
        caches:
         - dotnetcore
        script:
          - echo "test"
          - cd emri-api-core
          - dotnet add emri-api-core.sln package Newtonsoft.Json
          - dotnet restore
          - dotnet build